# Pinned to the original Code Ocean capsule (capsule-0769244) for reproducibility.
# (These match the env you already run in, so versions are NOT the cause of the
# observed alpha-direction difference — that is unseeded-init variance in a
# fragile policy; seed the run to compare implementations head-to-head.)
#
# Target interpreter: CPython 3.7.3 (matches the cloud reproduction box).

torch==1.2.0
numpy==1.18.1
scipy==1.3.1
pandas==0.25.3

gym==0.15.6
matplotlib==3.1.1

# I/O for poloniex_fc.hf (HDF5)
tables==3.6.1
h5py==2.9.0

# logging
tensorboardX==2.0
tensorboard-logger==0.0.4
